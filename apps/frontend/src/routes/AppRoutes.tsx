import { Routes, Route } from "react-router";

const AppRoutes = () => {
  return (
    <Routes>
      <Route path="/" element={<div>Main page</div>} />
    </Routes>
  );
};

export default AppRoutes;
